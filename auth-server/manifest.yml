---
applications:
- name: ll-auth-server
  memory: 700M
  instances: 1 
  path: target/auth-server-0.0.1-SNAPSHOT.war
  random-route: true
  env:
    RAILS_ENV: production
    RACK_ENV: production
  services:
   - instance1
