spring:
  application:
    name: auth-server
  cloud:
    config:
      discovery: 
        enable: true
        service-id: CONFIG-SERVER
      profile: oauth2
#  cloud:
#    config:
#      uri: ${CONFIG_SERVER_URL:http://localhost:8888}
  profiles:
    active: dev
      

