spring:
  application:
    name: api-gateway
  cloud:
    config:
      discovery: 
        enable: true
        service-id: CONFIG-SERVER
      profile: dev
#  profiles:
#    active: zuul_dev
#      uri: ${CONFIG_SERVER_URL:http://localhost:8888}



